node_modules/
.DS_Store
out/
release*/

# Local GitCode deploy credentials
scripts/deploy-release.conf

# Keep the generated raster icon in version control for builds without SVG tools.
runtime/
runtime.tar.gz
*.log
